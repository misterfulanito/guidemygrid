---
phase: 04-release-automation-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/store.types.ts
  - src/store/uiStore.ts
  - src/App.tsx
  - src/__tests__/uiStore.updateInfo.test.ts
  - src/__tests__/App.updateWiring.test.ts
  - src/__tests__/updateBanner.download.test.ts
autonomous: true
requirements: [UPD-02, UPD-03]
must_haves:
  truths:
    - "App.tsx calls checkForUpdates() inside a mount useEffect (fires every panel open, no throttle) and stores a returned hasUpdate result via setUpdateInfo"
    - "uiStore exposes updateInfo (UpdateInfo | null) with setUpdateInfo and dismissUpdate; dismissUpdate clears it to null"
    - "App.tsx conditionally renders <UpdateBanner> only when updateInfo is set; the banner's sole external action is uxp shell.openExternal (no in-app download/install)"
  artifacts:
    - src/store/uiStore.ts
    - src/types/store.types.ts
    - src/App.tsx
    - src/__tests__/uiStore.updateInfo.test.ts
    - src/__tests__/App.updateWiring.test.ts
    - src/__tests__/updateBanner.download.test.ts
  key_links:
    - "App useEffect → checkForUpdates() → uiStore.setUpdateInfo → conditional <UpdateBanner> render (reconnects the dead code path, UPD-03)"
    - "UpdateBanner openRelease → require('uxp').shell.openExternal (manual browser download, UPD-02)"
---

<objective>
Reconnect the currently-dead in-app update checker end-to-end (UPD-03): wire the already-built `checkForUpdates()` and `<UpdateBanner>` into `App.tsx` via a mount effect and new `uiStore` state, so the update notification actually fires instead of just existing in source. Preserve the existing manual "click → browser download" flow (UPD-02) unchanged — this is wiring, not rebuilding.

Purpose: Turn two correct-but-orphaned pieces of code (`updateChecker.ts`, `UpdateBanner.tsx`) into a live feature, following this project's own `useDocument.ts` mount-effect pattern and `uiStore` flat-field convention.
Output: Extended `uiStore`/`UIStore` type with update state; `App.tsx` mount wiring + conditional render; behavioral test for the store lifecycle; static regression guards proving the wiring exists (App calls the checker) and that the banner's only action remains `shell.openExternal`.
</objective>

<execution_context>
@/Users/hurisb/.claude/gsd-core/workflows/execute-plan.md
@/Users/hurisb/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-automation-distribution/04-RESEARCH.md
@.planning/phases/04-release-automation-distribution/04-PATTERNS.md
@src/App.tsx
@src/store/uiStore.ts
@src/types/store.types.ts
@src/services/updateChecker.ts
@src/components/shared/UpdateBanner.tsx
@src/hooks/useDocument.ts
</context>

## Artifacts this phase produces (Plan 02)

- `UIStore` interface (in `src/types/store.types.ts`) gains fields: `updateInfo: UpdateInfo | null`, `setUpdateInfo: (info: UpdateInfo | null) => void`, `dismissUpdate: () => void` (with `UpdateInfo` imported from `../services/updateChecker`)
- `useUIStore` (in `src/store/uiStore.ts`) gains runtime `updateInfo` field + `setUpdateInfo` + `dismissUpdate` setters
- `src/App.tsx` gains: imports of `checkForUpdates` and `UpdateBanner`, a mount `useEffect` calling `checkForUpdates()`, and a conditional `<UpdateBanner info={updateInfo} onDismiss={dismissUpdate} />` render
- New files: `src/__tests__/uiStore.updateInfo.test.ts`, `src/__tests__/App.updateWiring.test.ts`, `src/__tests__/updateBanner.download.test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add updateInfo state to uiStore + UIStore type, with behavioral test</name>
  <read_first>
    - src/store/uiStore.ts (full 15-line file — copy the flat-field + single-setter shape exactly; `setError` is the closest analog since UpdateInfo is replaced wholesale, no shallow-merge)
    - src/types/store.types.ts (existing UIStore interface, lines 29-38 — extend it, don't replace)
    - src/services/updateChecker.ts (line 8 — `UpdateInfo` is exported here; import it into store.types.ts rather than redefining)
    - src/__tests__/documentWatcher.test.ts (in-repo test scaffolding discipline for beforeEach/afterEach and describe naming)
    - .planning/phases/04-release-automation-distribution/04-PATTERNS.md — "src/store/uiStore.ts (store, CRUD)" section for the exact add-shape
  </read_first>
  <behavior>
    - setUpdateInfo({hasUpdate,latestVersion,downloadUrl}) then getState().updateInfo deep-equals that object
    - dismissUpdate() sets getState().updateInfo back to null
    - initial getState().updateInfo is null
  </behavior>
  <action>
    In `src/types/store.types.ts`: import the `UpdateInfo` type from `../services/updateChecker` at the top, then extend the `UIStore` interface with three members — `updateInfo: UpdateInfo | null`, `setUpdateInfo: (info: UpdateInfo | null) => void`, and `dismissUpdate: () => void`. Keep the existing members unchanged.

    In `src/store/uiStore.ts`: add `updateInfo: null` to the initial state object, and add two setters following the existing flat-replace style (matching `setError: (message) => set({ lastError: message })`) — `setUpdateInfo: (info) => set({ updateInfo: info })` and `dismissUpdate: () => set({ updateInfo: null })`. Do NOT use a shallow-merge setter here; `UpdateInfo` is always replaced wholesale, not partially patched.

    Create `src/__tests__/uiStore.updateInfo.test.ts` (first line `// src/__tests__/uiStore.updateInfo.test.ts`), describe block `uiStore update state (UPD-03)`. Import `useUIStore` from `../store` (the barrel re-exports it). Test purely through the vanilla store API `useUIStore.getState()` — no React render needed. Reset state in `afterEach` by calling `useUIStore.getState().dismissUpdate()`. Assert the three behaviors listed above using a fixed `UpdateInfo` fixture (hasUpdate: true, latestVersion: '9.9.9', downloadUrl: 'https://github.com/misterfulanito/guidemygrid/releases/latest').
  </action>
  <verify>
    <automated>npx jest src/__tests__/uiStore.updateInfo.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/uiStore.updateInfo.test.ts` passes: initial `updateInfo` is null; `setUpdateInfo(fixture)` stores it; `dismissUpdate()` clears it to null.
    - `npm run type-check` (`tsc --noEmit`) passes — `UpdateInfo` is imported (not redefined) and the `UIStore` interface and `useUIStore` implementation agree.
    - `src/store/uiStore.ts` uses a flat-replace setter for `updateInfo` (no `...state` spread on this field).
  </acceptance_criteria>
  <done>uiStore holds update-notification state with a type-checked, behaviorally-tested get/set/dismiss lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire checkForUpdates + UpdateBanner into App.tsx, with wiring + UPD-02 regression guards</name>
  <read_first>
    - src/App.tsx (full file — add two imports; add one mount useEffect; add one conditional render next to the existing DocumentHintBanner block at lines 68-71; App already imports useEffect and useUIStore)
    - src/hooks/useDocument.ts (the cancellation-guard async-in-useEffect shape to model the mount effect on)
    - src/services/updateChecker.ts (checkForUpdates returns Promise<UpdateInfo | null>, already swallows all errors and resolves null — never wrap its .then in try/catch)
    - src/components/shared/UpdateBanner.tsx (existing component: props are `{ info: UpdateInfo; onDismiss: () => void }`; its `openRelease` calls `require('uxp').shell.openExternal(info.downloadUrl)` inside try/catch — this is the UPD-02 behavior the static guard must protect; do NOT modify this file)
    - src/__tests__/installer-retirement.test.ts (the in-repo static-source-scan pattern: read a file with fs.readFileSync and assert on its text — mirror this for the wiring and banner guards)
    - .planning/phases/04-release-automation-distribution/04-PATTERNS.md — "src/App.tsx (component, event-driven)" section (imports, mount-effect, conditional-render snippets to copy exactly)
    - .planning/phases/04-release-automation-distribution/04-RESEARCH.md — "App.tsx wiring (minimal diff)" and Anti-Patterns (no throttle/cache; no in-app download)
  </read_first>
  <behavior>
    - App.updateWiring.test.ts: App.tsx source imports checkForUpdates from './services/updateChecker', calls checkForUpdates( inside a useEffect, and renders <UpdateBanner
    - updateBanner.download.test.ts: UpdateBanner.tsx source invokes shell.openExternal and contains no filesystem-write or in-app-download call
  </behavior>
  <action>
    In `src/App.tsx`: add `import { checkForUpdates } from './services/updateChecker';` and `import { UpdateBanner } from './components/shared/UpdateBanner';` (relative imports, no alias). Pull `updateInfo`, `setUpdateInfo`, and `dismissUpdate` from `useUIStore()` alongside the existing `guidesVisible`/`setGuidesVisible` destructure. Add a mount `useEffect` with an empty dependency array that, per D-04, fires on every panel open with no throttle, no 24-hour cache, and no manual "check now" button: model it on `useDocument.ts`'s cancellation-guard shape — declare a local `cancelled = false`, call `checkForUpdates().then((info) => { if (!cancelled && info?.hasUpdate) setUpdateInfo(info); })`, and return a cleanup that sets `cancelled = true`. Do NOT wrap the `.then` in a try/catch — `checkForUpdates()` already resolves null on every failure per the project's silent-null convention; adding error handling here would deviate from it.

    Add the conditional render immediately after the existing `DocumentHintBanner` block (App.tsx lines 68-71), using the same shape: render `<UpdateBanner info={updateInfo} onDismiss={dismissUpdate} />` only when `updateInfo` is truthy. Add a `{/* ── Update-available banner (dismissible) ── */}` section comment matching the file's existing JSX comment style.

    Do NOT modify `UpdateBanner.tsx` — it is imported and rendered as-is; its `shell.openExternal` manual-download behavior (UPD-02) must remain untouched.

    Create two static-source-scan tests mirroring `installer-retirement.test.ts` (read the file text with `fs.readFileSync(path.resolve(__dirname, ...))`, assert with `.toContain(...)` / `.not.toContain(...)`):
    - `src/__tests__/App.updateWiring.test.ts` (describe `App update-checker wiring (UPD-03)`): assert `src/App.tsx` text contains an import from `./services/updateChecker`, contains a `checkForUpdates(` call, and contains `<UpdateBanner`. This guards against the wiring being deleted and the checker becoming dead code again.
    - `src/__tests__/updateBanner.download.test.ts` (describe `UpdateBanner manual-download flow (UPD-02)`): assert `src/components/shared/UpdateBanner.tsx` text contains `openExternal` (the UXP shell browser-open call), and assert it does NOT contain a filesystem-write or auto-install surface — assert absence of `writeFile` and absence of `require('fs')`. This guards UPD-02's "manual browser download, no silent/automatic install" requirement.
  </action>
  <verify>
    <automated>npx jest src/__tests__/App.updateWiring.test.ts src/__tests__/updateBanner.download.test.ts && npm run type-check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` imports `checkForUpdates` and `UpdateBanner`, calls `checkForUpdates()` inside a mount `useEffect`, and conditionally renders `<UpdateBanner>` gated on `updateInfo`.
    - `src/__tests__/App.updateWiring.test.ts` passes and fails if any of the three wiring tokens (updateChecker import, `checkForUpdates(` call, `<UpdateBanner` render) are removed from `App.tsx`.
    - `src/__tests__/updateBanner.download.test.ts` passes: asserts `openExternal` present and `writeFile`/`require('fs')` absent in `UpdateBanner.tsx`.
    - `npm run type-check` passes and full `npm test` is green.
    - `git diff src/components/shared/UpdateBanner.tsx` is empty (banner unchanged — UPD-02 not regressed).
  </acceptance_criteria>
  <done>Opening the panel now runs the update check on mount and shows the existing banner when a newer release exists; the manual-download-only behavior is regression-guarded; the previously-dead code path is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel JS → api.github.com | checkForUpdates() consumes untrusted release JSON (validated in Plan 01's covered logic) |
| UpdateBanner → uxp shell | The only egress from the banner is opening an external URL in the system browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering/Spoofing | App wiring passing info.downloadUrl to the banner | high | mitigate | Wiring must not bypass `isSafeUrl` — App only forwards the already-validated `UpdateInfo` from `checkForUpdates()`; no new URL construction is introduced |
| T-04-04 | Elevation of Privilege | UpdateBanner action surface | high | mitigate | `updateBanner.download.test.ts` asserts the banner's only external action is `shell.openExternal` and no filesystem-write/auto-install surface exists (UPD-02 "no silent install") |

No package-manager installs occur in this phase — no supply-chain (T-04-SC) install checkpoint applies.
</threat_model>

<verification>
- `npm test` green (full suite including the three new test files).
- `npm run type-check` green (UIStore interface and useUIStore implementation agree; UpdateInfo imported not redefined).
- `App.tsx` mount effect uses an empty dependency array (fires every mount, no throttle — D-04) and does not wrap `checkForUpdates()` in try/catch.
</verification>

<success_criteria>
- The update checker is invoked on panel open and its result flows through uiStore into a conditionally-rendered banner (UPD-03).
- The manual browser-download behavior is preserved and regression-guarded (UPD-02).
</success_criteria>

<output>
Create `.planning/phases/04-release-automation-distribution/04-02-SUMMARY.md` when done
</output>
