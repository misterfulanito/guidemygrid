---
phase: 04-release-automation-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - src/__tests__/updateChecker.test.ts
  - src/__tests__/manifest-permissions.test.ts
  - src/__tests__/installer-retirement.test.ts
autonomous: true
requirements: [UPD-01, UPD-03, DIST-01]
must_haves:
  truths:
    - "checkForUpdates() rejects any release/asset URL that does not start with https://github.com/misterfulanito/guidemygrid/ (isSafeUrl allowlist), returning null"
    - "checkForUpdates() returns null (never throws) on network failure, non-2xx response, or malformed/non-semver payload"
    - "manifest.json declares requiredPermissions.network.domains equal to exactly [\"https://api.github.com\"] — never \"all\""
  artifacts:
    - src/__tests__/updateChecker.test.ts
    - src/__tests__/manifest-permissions.test.ts
    - manifest.json
  key_links:
    - "manifest.json requiredPermissions.network ↔ Creative Cloud Desktop install-time network consent (D-03 accepted admin-password prompt)"
    - "updateChecker.ts ALLOWED_URL / api.github.com allowlist ↔ DIST-01 canonical GitHub host"
---

<objective>
Close the zero-coverage test gap on the existing (correct but untested) update checker, and consciously re-declare the `requiredPermissions.network` manifest block that Phase 1 removed — the single decision this phase has been carrying since Phase 1 (D-01/D-02/D-03). Research resolved D-02 (avoiding the permission) as a dead end; D-03 (accept the install-time admin-password prompt in exchange for a working update checker) is the confirmed path.

Purpose: Make the checker's security-relevant logic (domain allowlist, semver validation, silent-null-on-error) provable by test, and permit the network call the checker needs — without broadening scope beyond the single GitHub API domain.
Output: New `updateChecker.test.ts` and `manifest-permissions.test.ts`; `manifest.json` with the scoped permission restored; the obsolete "no requiredPermissions" assertion removed from `installer-retirement.test.ts`.
</objective>

<execution_context>
@/Users/hurisb/.claude/gsd-core/workflows/execute-plan.md
@/Users/hurisb/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-automation-distribution/04-RESEARCH.md
@.planning/phases/04-release-automation-distribution/04-PATTERNS.md
@src/services/updateChecker.ts
@manifest.json
@src/__tests__/installer-retirement.test.ts
@src/__tests__/checksums.test.ts
</context>

## Artifacts this phase produces (Plan 01)

- `manifest.json` new top-level key: `requiredPermissions.network.domains: ["https://api.github.com"]`
- New file `src/__tests__/updateChecker.test.ts` (Jest suite for `checkForUpdates`)
- New file `src/__tests__/manifest-permissions.test.ts` (static JSON regression guard on the manifest permission scope)
- Removed assertion: the `it('manifest.json should not declare requiredPermissions', ...)` block in `src/__tests__/installer-retirement.test.ts`

<tasks>

<task type="auto">
  <name>Task 1: Add fetch-mocked unit tests for checkForUpdates (UPD-01, DIST-01)</name>
  <read_first>
    - src/services/updateChecker.ts (the module under test — note it uses global `fetch`, exports `checkForUpdates`/`UpdateInfo`, and the constants GITHUB_REPO/SEMVER_RE/ALLOWED_URL; `validateRelease`/`isSafeUrl`/`compareVersions` are internal, exercised via `checkForUpdates`)
    - src/__tests__/checksums.test.ts (in-repo describe/it style, Node-built-in require pattern, temp-file discipline)
    - src/__tests__/documentWatcher.test.ts (in-repo beforeEach/afterEach setup+teardown discipline to mirror for the fetch mock reset)
    - .planning/phases/04-release-automation-distribution/04-RESEARCH.md — "Code Examples > updateChecker.ts test skeleton" (use as the verbatim starting point) and "Pitfall 4" (global.fetch mocking in testEnvironment: node)
    - src/version.ts (PLUGIN_VERSION is currently '0.1.0' — version-compare fixtures must use a tag_name higher than this to assert hasUpdate: true)
  </read_first>
  <action>
    Create `src/__tests__/updateChecker.test.ts`. First line must be the path comment `// src/__tests__/updateChecker.test.ts`. Import `checkForUpdates` from `../services/updateChecker` (relative import — no path alias). Name the top describe block `checkForUpdates (UPD-01, DIST-01, UPD-03)` per the repo's `describe('<subject> (<REQ-IDs>)')` convention.

    Because jest runs `testEnvironment: "node"` and Node exposes a real global `fetch`, mock it explicitly (no existing repo precedent — this is new ground per D-02/Pitfall 4): in `beforeEach` set `global.fetch = jest.fn()`; in `afterEach` call `jest.restoreAllMocks()`. Never rely on an auto-mock and never let a test reach the real network.

    Cover these behaviors (each an `it`), driving them purely through mocked `fetch` resolved/rejected values:
    - hasUpdate true: `fetch` resolves `{ ok: true, json: async () => ({ tag_name: 'v99.0.0', html_url: 'https://github.com/misterfulanito/guidemygrid/releases/tag/v99.0.0', assets: [] }) }` → `(await checkForUpdates())?.hasUpdate === true` and `latestVersion === '99.0.0'`.
    - allowlist rejection (UPD-01/DIST-01): `html_url` set to an off-domain host (e.g. an `evil.example.com` URL) → `checkForUpdates()` resolves `null` (validateRelease throws on the domain check, caught → null).
    - semver rejection (UPD-01): `tag_name: 'not-a-version'` with an otherwise-valid github.com `html_url` → resolves `null`.
    - non-2xx: `fetch` resolves `{ ok: false }` → resolves `null` (never throws).
    - network failure (silent-null convention): `fetch` rejects `new Error('network down')` → `await expect(checkForUpdates()).resolves.toBeNull()`.
    - asset selection stays on-domain: when `assets` contains a `.ccx` whose `browser_download_url` is off-domain, the returned `downloadUrl` falls back to the validated `html_url` rather than the off-domain asset URL.

    Do NOT modify `updateChecker.ts` — these tests pass against the existing, unchanged module (they document already-correct behavior; UPD-01 says "don't regress this"). No fenced code in production files; this is a test-only task.
  </action>
  <verify>
    <automated>npx jest src/__tests__/updateChecker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/__tests__/updateChecker.test.ts` passes with at least 6 `it` cases green.
    - The suite sets `global.fetch = jest.fn()` in `beforeEach` and resets mocks in `afterEach` (no real network call — suite runs offline).
    - An off-domain `html_url` fixture produces a `null` result (allowlist enforced).
    - A non-semver `tag_name` fixture produces a `null` result.
    - A rejected `fetch` produces a resolved `null` (function never rejects).
    - `updateChecker.ts` is unchanged (git diff shows no source modification, only the new test file).
  </acceptance_criteria>
  <done>All six behaviors are covered and green; the checker's allowlist, semver validation, and silent-null contract are now regression-guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Restore scoped network permission in manifest.json + regression guard (UPD-03, D-01/D-03)</name>
  <read_first>
    - manifest.json (full current file — the permission is inserted as a new top-level sibling key alongside manifestVersion, id, name, version, main, host, entrypoints, icons; there is currently NO requiredPermissions block)
    - src/__tests__/installer-retirement.test.ts (contains an obsolete `it('manifest.json should not declare requiredPermissions', ...)` block at the end asserting `manifest.requiredPermissions` is undefined — this becomes false and must be removed; the fs/path imports stay used by the other two assertions)
    - src/__tests__/macos-installer-retirement.test.ts (static-file JSON-assertion pattern to mirror for the new manifest-permissions test)
    - .planning/phases/04-release-automation-distribution/04-RESEARCH.md — "manifest.json restoration (exact revert of commit d07142d)" and Security Domain "Elevation of Privilege" row (scope must stay a single-domain array)
    - .planning/phases/04-release-automation-distribution/04-CONTEXT.md — D-01/D-02/D-03 (accept the admin-password prompt; avoidance is a resolved dead end — do not re-investigate)
  </read_first>
  <action>
    Add a top-level `requiredPermissions` key to `manifest.json` with `network.domains` set to the single-element array containing exactly the GitHub API origin `https://api.github.com`. This is the exact scoped block that existed before Phase 1's commit `d07142d` removed it. Per D-01/D-03 this reintroduces Creative Cloud Desktop's install-time admin-password prompt — that is an accepted, documented consequence of shipping a working update checker, NOT a regression to engineer around (D-02 avoidance is a resolved dead end per RESEARCH; do not re-open it). Preserve strict scoping: the domains array must contain only the single GitHub API origin. Broadening it to the wildcard scope is forbidden — it gains nothing (nothing else is contacted) and expands the plugin's declared network surface beyond what the UXP consent dialog describes to the user.

    Remove the now-false assertion in `src/__tests__/installer-retirement.test.ts`: delete only the `it('manifest.json should not declare requiredPermissions', ...)` block (the last `it` in that describe). Leave the two Windows-retirement `it` blocks and the fs/path imports intact.

    Create `src/__tests__/manifest-permissions.test.ts` as the new single source of truth for the manifest permission scope. First line `// src/__tests__/manifest-permissions.test.ts`. Mirror the static-JSON-assertion shape of `macos-installer-retirement.test.ts`: `JSON.parse(fs.readFileSync(path.resolve(__dirname, '../../manifest.json'), 'utf8'))`, then assert `parsed.requiredPermissions.network.domains` deep-equals the single-element array of the GitHub API origin, and assert that array does not contain the wildcard scope value. Name the describe `manifest network permission (UPD-03, D-01/D-03)`.
  </action>
  <verify>
    <automated>npx jest src/__tests__/manifest-permissions.test.ts src/__tests__/installer-retirement.test.ts && node -e "const m=require('./manifest.json'); if(JSON.stringify(m.requiredPermissions.network.domains)!==JSON.stringify(['https://api.github.com'])) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` parses as valid JSON and `requiredPermissions.network.domains` deep-equals `["https://api.github.com"]`.
    - `src/__tests__/manifest-permissions.test.ts` passes and its assertion fails if the domains array is ever broadened to the wildcard or a second domain is added.
    - `src/__tests__/installer-retirement.test.ts` passes with the obsolete `requiredPermissions`-undefined `it` block removed and its other two assertions unchanged.
    - Full suite `npm test` is green.
  </acceptance_criteria>
  <done>The manifest declares the narrowly-scoped network permission; the regression guard prevents scope creep; no test still asserts the (now-false) "no permissions" state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel JS → api.github.com | Untrusted network response (release JSON) crosses into the plugin |
| manifest.json → Creative Cloud Desktop | Declared permission crosses into install-time consent; determines the admin-password prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering/Spoofing | checkForUpdates() download URL | high | mitigate | `isSafeUrl()` prefix-allowlist on `html_url` and asset URLs; Task 1 adds the off-domain-rejection test proving a malicious response cannot redirect the download link |
| T-04-02 | Elevation of Privilege | manifest.json requiredPermissions | medium | mitigate | Keep `network.domains` to exactly `["https://api.github.com"]`; Task 2's `manifest-permissions.test.ts` fails on any broadening to wildcard/extra domains |
| T-04-03 | Tampering | compareVersions on tag_name | medium | mitigate | `SEMVER_RE` rejects non-strict-semver tags before comparison; Task 1 adds the non-semver-rejection test |

No package-manager installs occur in this phase — no supply-chain (T-04-SC) install checkpoint applies.
</threat_model>

<verification>
- `npm test` green (full suite, including the two new test files and the edited retirement test).
- `manifest.json` is valid JSON with the scoped network permission present.
- `git diff src/services/updateChecker.ts` is empty (checker logic untouched — UPD-01 "don't regress").
</verification>

<success_criteria>
- The update checker's allowlist, semver, and silent-null behaviors are covered by passing tests (UPD-01, DIST-01).
- The manifest declares network access scoped to exactly the GitHub API origin and is guarded against scope creep (UPD-03 packaging half).
</success_criteria>

<output>
Create `.planning/phases/04-release-automation-distribution/04-01-SUMMARY.md` when done
</output>
