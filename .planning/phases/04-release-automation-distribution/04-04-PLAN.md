---
phase: 04-release-automation-distribution
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: []
autonomous: false
requirements: [UPD-01, UPD-02, UPD-03]
user_setup:
  - service: photoshop-creative-cloud-desktop
    why: "Only real Photoshop + Creative Cloud Desktop can exercise install-time consent and the live update-check on panel open"
    dashboard_config:
      - task: "Build the .ccx and install it via Creative Cloud Desktop on macOS"
        location: "npm run package:ccx → drag the built .ccx onto Creative Cloud Desktop"
must_haves:
  truths:
    - "Installing the built .ccx via Creative Cloud Desktop on macOS shows the admin-password prompt — the accepted, documented consequence of D-03 (restored network permission), not a bug"
    - "Opening the GuideMyGrid panel runs a live update check against api.github.com with no error in the console"
    - "When the latest GitHub Release is newer than the installed version, the UpdateBanner appears and clicking Download opens the system browser to the GitHub release page (manual, non-silent)"
  artifacts: []
  key_links:
    - "manifest requiredPermissions.network → CC Desktop install-time consent (admin prompt) → the plugin is permitted to call api.github.com → banner fires (UPD-03 end-to-end)"
---

<objective>
Confirm end-to-end, in real Photoshop, that the reconnected update checker actually fires and that the accepted install-time tradeoff behaves as researched. This is the phase gate the automated suite cannot cover: a live `.ccx` install through Creative Cloud Desktop (which only a human can drive) plus a real panel-open update check. It also re-verifies research Assumption A2 — that re-declaring the scoped network permission reproduces Phase 1's admin-password prompt behavior on the current CC Desktop version.

Purpose: Prove UPD-03 is live (not just wired in source) and that the D-03 admin-prompt consequence is present and expected — while confirming UPD-02's manual browser-download path still works from a real host.
Output: Human confirmation (or a defect report) covering the install-time prompt, the on-open update check, and the banner's manual-download action.
</objective>

<execution_context>
@/Users/hurisb/.claude/gsd-core/workflows/execute-plan.md
@/Users/hurisb/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-release-automation-distribution/04-RESEARCH.md
@.planning/phases/04-release-automation-distribution/04-CONTEXT.md
@manifest.json
@src/App.tsx
</context>

## Artifacts this phase produces (Plan 04)

- No source artifacts (verification-only plan). Produces a human-verify record of the live install-time prompt, the on-open update check, and the banner's manual-download behavior. macOS-scoped; Windows admin-prompt parity is flagged as an explicit open item (Research Open Question 2).

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live macOS install + panel-open update-check verification (UPD-01, UPD-02, UPD-03)</name>
  <action>Human-only step (Creative Cloud Desktop cannot be driven headlessly). The user builds the `.ccx` (`npm run build` then `npm run package:ccx`), installs it via Creative Cloud Desktop on macOS, confirms the admin-password prompt appears on install (the accepted D-03 consequence of the restored network permission — NOT a regression), opens the GuideMyGrid panel to trigger the on-mount update check, confirms no `[GMG] checkForUpdates failed` console error, and confirms the UpdateBanner either appears with a working browser-opening Download (when a newer release exists, UPD-02) or correctly stays hidden (when up to date). Windows admin-prompt parity is explicitly out of scope here (Research Open Question 2).</action>
  <what-built>
    Plan 01 restored `manifest.json`'s scoped `requiredPermissions.network.domains: ["https://api.github.com"]` (D-01/D-03), and Plan 02 wired `checkForUpdates()` + `<UpdateBanner>` into `App.tsx` to fire on every panel open (D-04). This checkpoint verifies the combined result on a real macOS Photoshop install — the one thing CI/Jest cannot exercise (Creative Cloud Desktop cannot be driven headlessly).
  </what-built>
  <how-to-verify>
    1. Build the package: run `npm run build` then `npm run package:ccx` (produces the `.ccx` under `releases/`). If `package:ccx` errors on this machine, use the standard `.ccx` build path this project already uses for real installs.
    2. Install the built `.ccx` via Creative Cloud Desktop (drag it on, or use its "Manage plugins" install flow).
    3. EXPECTED and CORRECT: Creative Cloud Desktop shows an admin-password prompt during install/update, because the manifest now declares a network permission. This is the accepted D-03 tradeoff — approve it. Do NOT treat the prompt as a regression; its return is the expected consequence of shipping a working update checker (its absence would mean the permission did not take effect). Note whether the prompt appeared.
    4. Open Photoshop → Plugins → GuideMyGrid so the panel mounts. Open the UXP developer console if convenient and confirm there is no `[GMG] checkForUpdates failed` error on open (a successful, silent check leaves no error).
    5. Update-banner behavior: if the newest GitHub Release for this repo is a HIGHER version than the installed build, confirm the blue "New version available" banner appears at the top of the panel, that clicking "Download" opens the system browser to the GitHub release page (UPD-02 — manual, not an in-app download), and that the × dismiss button hides it. If the installed build already equals the latest release (no newer version exists), confirm instead that NO banner appears and no console error occurs — that is the correct "up to date" state.
    Expected outcome: admin prompt appears on install (expected), panel open triggers a silent successful check, and the banner correctly appears-with-working-download OR correctly stays hidden depending on whether a newer release exists.
  </how-to-verify>
  <resume-signal>Type "approved" with a one-line note on (a) whether the admin prompt appeared, (b) no console error on open, and (c) banner shown+Download-opened-browser OR correctly hidden (up to date). Otherwise describe the defect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json → Creative Cloud Desktop | Install-time consent; the declared network permission drives the admin-password prompt |
| Live panel → api.github.com | Real (unmocked) network response consumed by the validated checker on open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Elevation of Privilege | CC Desktop install-time consent (admin prompt) | medium | accept | D-03 (locked): the admin-password prompt is the accepted cost of a working update checker; the checkpoint confirms the prompt is scoped to install-time consent, not a broader elevation, and that the declared domain remains the single GitHub API origin |
| T-04-01 | Tampering/Spoofing | Live checkForUpdates download URL | high | mitigate | Verified live: Download opens only the GitHub release page (isSafeUrl allowlist from Plan 01), never an off-domain or in-app download |

No package-manager installs occur in this phase — no supply-chain (T-04-SC) install checkpoint applies.
</threat_model>

<verification>
- Human confirms: admin-password prompt appears on install (expected, D-03); no `[GMG]` console error on panel open; banner appears with a working browser-opening Download when a newer release exists, or correctly stays hidden when up to date.
- Windows admin-prompt parity is explicitly NOT verified here (no physical Windows machine) — flagged as an open item per Research Open Question 2.
</verification>

<success_criteria>
- The reconnected update checker is proven live end-to-end on macOS (UPD-03), the manual browser-download path works from a real host (UPD-02), and the accepted install-time admin prompt (D-03) is confirmed present and expected.
</success_criteria>

<output>
Create `.planning/phases/04-release-automation-distribution/04-04-SUMMARY.md` when done
</output>
