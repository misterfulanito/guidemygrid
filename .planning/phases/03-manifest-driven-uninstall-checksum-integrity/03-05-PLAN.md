---
phase: 03-manifest-driven-uninstall-checksum-integrity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/photoshopBridge.ts
  - src/__tests__/photoshopBridge.getActiveDocument.test.ts
autonomous: true
gap_closure: true
requirements: [GAP-03-DOC-DETECT]

must_haves:
  truths:
    - "The panel reports an open document as present immediately on mount, even before UXP grants modal scope — a failed selection check no longer nulls out document presence (fixes UAT Gap: document/canvas not detected until a marquee/select-all is made)"
    - "getActiveDocument() returns a valid DocumentInfo with hasSelection:false when the selection check throws, instead of rejecting the whole call"
    - "A Jest test locks in this resilience so the regression cannot silently return"
  artifacts:
    - "src/services/photoshopBridge.ts (getActiveDocument hardened against selection-check failure)"
    - "src/__tests__/photoshopBridge.getActiveDocument.test.ts"
  key_links:
    - "getActiveDocument()'s hasActiveSelection() call is wrapped in its own try/catch defaulting to false"
    - "useDocument.ts refresh() no longer receives a rejection from getActiveDocument() caused by a selection-only failure, so it keeps `document` set when a doc is genuinely open"
---

<objective>
Fix a pre-existing bug (surfaced during Phase 3 UAT, out of Phase 3's scope, in files no 03-* commit touched): the panel does not detect the active document/canvas until the user makes a marquee or select-all selection. The Top/Left/Right/Bottom margin fields and the Add Guides button stay inert until then.

Root cause (from 03-UAT.md Gaps): `getActiveDocument()` computes `hasSelection` by calling `hasActiveSelection()` → `getSelectionBounds()`, which wraps a `batchPlay` call in `photoshop.core.executeAsModal()`. On initial panel mount UXP has not yet granted the panel modal scope, so `executeAsModal` can throw. That exception is not caught inside `getActiveDocument()`/`hasActiveSelection()`, so it bubbles up to `useDocument.ts`'s `refresh()` catch block, which sets `document` to `null` even though a document is genuinely open. A later Photoshop notification (e.g. Cmd+A) re-runs `refresh()`; by then modal scope exists and detection succeeds — which is exactly why a selection "wakes up" the panel.

Fix: make the selection check non-fatal to document detection. A modal-scope timing failure should degrade only the `hasSelection` flag, never document presence.

Purpose: Restore correct on-mount document detection so a designer can use the panel without first making a dummy selection.
Output: A hardened `getActiveDocument()` plus a regression test that proves the resilience.
</objective>

<execution_context>
@/Users/hurisb/.claude/gsd-core/workflows/execute-plan.md
@/Users/hurisb/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-manifest-driven-uninstall-checksum-integrity/03-UAT.md

# Files being fixed / referenced
@src/services/photoshopBridge.ts
@src/hooks/useDocument.ts
@src/types/photoshop.types.ts

# Test-style reference (existing suite, ts-jest, testEnvironment: node)
@src/__tests__/gridGenerator.sideGuide.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden getActiveDocument against selection-check failure + lock it with a regression test</name>
  <files>src/services/photoshopBridge.ts, src/__tests__/photoshopBridge.getActiveDocument.test.ts</files>
  <behavior>
    Write the test FIRST (it must fail against the current code), then apply the fix and watch it pass.

    - Test 1 (the resilience case — currently FAILS): when `getSelectionBounds()` rejects (simulating an `executeAsModal` modal-scope failure on initial mount), `getActiveDocument()` still resolves to a non-null `DocumentInfo` with the document's id/name/width/height/resolution intact and `hasSelection: false`. Against the un-fixed code this rejects instead of resolving — that is the RED that proves the test catches the bug.
    - Test 2 (control — passes before and after): when `getSelectionBounds()` resolves to a bounds object, `getActiveDocument()` returns `hasSelection: true`.
    - Test 3 (control — passes before and after): when `app.activeDocument` is null, `getActiveDocument()` returns `null`.
  </behavior>
  <action>
    Step A — write the failing regression test at src/__tests__/photoshopBridge.getActiveDocument.test.ts.

    The `photoshop` module is supplied by the UXP host at runtime and is NOT an npm package, so it will not resolve under Jest. Mock it with a Jest virtual mock at the top of the file, before importing the bridge: call `jest.mock('photoshop', factory, { virtual: true })`. The factory must return an object shaped `{ app, core, action }` where:
      - `app` exposes an `activeDocument` getter that returns a module-scoped mutable variable named with a `mock` prefix (e.g. `mockActiveDocument`) so Jest's mock-factory hoisting permits the reference. Seed it with a plain document object: id 1, name 'Untitled-1', width 1000, height 1000, resolution 72.
      - `core.executeAsModal` and `action.batchPlay`/`addNotificationListener`/`removeNotificationListener` are `jest.fn()` stubs (only needed so module load succeeds; the tests drive selection state via a spy, not these).
    Import the singleton `photoshopBridge` from '../services/photoshopBridge'. In each test, control the selection outcome with `jest.spyOn(photoshopBridge as any, 'getSelectionBounds')` — `getSelectionBounds` is what `hasActiveSelection()` calls internally, and casting through `any` is required because it is a private method. Use `.mockRejectedValue(new Error('executeAsModal: modal scope not available'))` for the resilience case, `.mockResolvedValue({ top:0,left:0,bottom:1000,right:1000,width:1000,height:1000 })` for the with-selection control, and reassign the `mockActiveDocument` variable to null for the no-document control. Restore mocks between tests (`afterEach(() => jest.restoreAllMocks())`). Assert exactly the behaviors listed in the behavior block. Follow the existing suite's plain `describe`/`test`/`expect` style (see gridGenerator.sideGuide.test.ts) — no semicolon or quote-style changes beyond what matches the file.

    Run `npx jest photoshopBridge.getActiveDocument` and confirm Test 1 fails (the call rejects) — this proves the test exercises the real bug.

    Step B — apply the minimal fix in src/services/photoshopBridge.ts `getActiveDocument()` (currently lines 14-25). After the early `if (!doc) return null;` guard, resolve `hasSelection` through a local try/catch: attempt `await this.hasActiveSelection()`, and on any thrown error default `hasSelection` to `false` and log the failure with the codebase's `[GMG]` prefix (mirror the existing fallback pattern in `getGuidesVisible()` at lines 104-124). Then return the same `DocumentInfo` object as today but using the guarded `hasSelection` value. Do NOT change `getSelectionBounds()`, `hasActiveSelection()`, or `useDocument.ts` — the UAT diagnosis confirms hardening `getActiveDocument()` alone is sufficient, and the brief is a targeted bug fix, not a refactor.

    Re-run the test and confirm all three cases pass (GREEN).
  </action>
  <verify>
    <automated>npx jest photoshopBridge.getActiveDocument --runInBand 2>&1 | tail -20 && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The new test suite passes: getActiveDocument() returns a non-null document with hasSelection:false when getSelectionBounds() rejects, hasSelection:true when it resolves, and null when there is no active document. type-check is clean. getSelectionBounds(), hasActiveSelection(), and useDocument.ts are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UXP host (Photoshop) → panel code | The panel receives document/selection data and modal-scope grants from the UXP runtime; timing of the modal grant is host-controlled and not trusted to be ready on mount |

No new trust boundary is introduced. This change touches only in-panel error handling for a host-timing race; no network, filesystem, IPC, or package-install surface is added or modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Denial of Service (self-inflicted) | getActiveDocument() on initial mount | low | mitigate | The fix itself removes the failure mode: a modal-scope timing exception no longer suppresses document detection, restoring panel availability without a dummy selection |
| T-03-05-02 | Information Disclosure (error masking) | try/catch swallowing the selection-check error | low | accept | The catch logs via `console.error('[GMG] ...')` rather than silently discarding, so a genuine (non-timing) selection failure remains diagnosable in the console; hasSelection simply degrades to false |

No `npm`/`pip`/`cargo` install occurs in this plan, so no supply-chain (T-*-SC) threat or package legitimacy checkpoint applies.
</threat_model>

<verification>
- `npx jest photoshopBridge.getActiveDocument` — all three cases green
- `npm test` — full suite still green (no regression in existing tests)
- `npx tsc --noEmit` — no type errors
- Manual confirmation deferred to UAT re-test: with a document open, the panel shows the document and enables the margin fields / Add Guides button on mount, without requiring a marquee or select-all first
</verification>

<success_criteria>
- getActiveDocument() reports a genuinely-open document even when the selection check throws (modal-scope timing on mount)
- hasSelection degrades to false on selection-check failure instead of nulling the document
- A regression test locks the behavior in and fails against the pre-fix code
- The fix is confined to getActiveDocument() in photoshopBridge.ts plus one new test file — no changes to getSelectionBounds(), hasActiveSelection(), or useDocument.ts
</success_criteria>

<output>
Create `.planning/phases/03-manifest-driven-uninstall-checksum-integrity/03-05-SUMMARY.md` when done
</output>
