---
phase: 01-foundation-macos-installer-rework
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - distribution/README.md
  - distribution/photoshop/macos/README.md
  - distribution/photoshop/windows/README.md
  - distribution/photoshop/windows/install.bat
  - distribution/photoshop/windows/install.ps1
  - distribution/photoshop/windows/install.sh
  - distribution/photoshop/windows/uninstall.bat
  - distribution/photoshop/windows/uninstall.ps1
  - release/version.js
  - release/github-release.js
  - package.json
autonomous: true
requirements: [FOUND-02]
must_haves:
  truths:
    - "distribution/photoshop/macos/, distribution/photoshop/windows/, and release/ directories all exist"
    - "release/version.js runs as the prebuild hook and still syncs package.json version into manifest.json + src/version.ts"
    - "The existing Windows installer scripts live under distribution/photoshop/windows/ with unchanged logic"
    - "`npm run build` still succeeds after the script relocations"
  artifacts:
    - "release/version.js (renamed from scripts/sync-version.js)"
    - "release/github-release.js (renamed from scripts/gh-release.js)"
    - "distribution/README.md, distribution/photoshop/macos/README.md, distribution/photoshop/windows/README.md"
  key_links:
    - "package.json prebuild → release/version.js"
    - "package.json publish:* → release/github-release.js"
---

<objective>
Establish the host-agnostic directory split required by FOUND-02: create `distribution/photoshop/{macos,windows}/` (host-specific packaging/install) and `release/` (host-agnostic release automation), relocate the scripts that already exist in host-agnostic form, relocate the existing Windows installer scripts unmodified into their Phase-2 home, and add READMEs that encode the split's intent. This makes a future Illustrator/Figma expansion never require touching shared release scripts.

Scope guardrails from RESEARCH.md:
- `release/` receives ONLY scripts that already exist and are already host-agnostic: `version.js` (renamed from scripts/sync-version.js) and `github-release.js` (renamed from scripts/gh-release.js). Do NOT stub `checksums.js` (Phase 3) or `gumroad-sync.js` (Phase 4) — they do not exist yet and stubbing them adds untested dead code.
- `distribution/photoshop/windows/` gets the existing Windows scripts relocated UNMODIFIED — do not touch their internal logic (that is Phase 2, WIN-01..05).
- The legacy macOS `.pkg` scripts (scripts/build-mac-pkg.js, scripts/pkg-resources/*, scripts/build-mac-uninstaller.js) stay in scripts/ untouched for now; the macOS installer replacement lands in Plans 03–04, which is also where the superseded installer build script is removed.

Purpose: Give host-specific and release-automation work stable, separated homes.
Output: The `distribution/` and `release/` trees with relocated scripts and READMEs; package.json script paths updated.
</objective>

<execution_context>
@/Users/hurisb/.claude/gsd-core/workflows/execute-plan.md
@/Users/hurisb/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-macos-installer-rework/01-RESEARCH.md
@.planning/phases/01-foundation-macos-installer-rework/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the directory split and relocate host-agnostic release scripts</name>
  <files>release/version.js, release/github-release.js, package.json</files>
  <read_first>
    - .planning/phases/01-foundation-macos-installer-rework/01-RESEARCH.md (the `## Architecture Patterns` → Recommended Project Structure and Structure Rationale)
    - .planning/phases/01-foundation-macos-installer-rework/01-PATTERNS.md (the `release/version.js` and `release/github-release.js` sections — verbatim rename guidance)
    - scripts/sync-version.js (source of the version.js rename)
    - scripts/gh-release.js (merged in via Plan 01 — source of the github-release.js rename; read to see the current `files` array)
    - package.json (scripts section — prebuild and publish:* references to update)
  </read_first>
  <action>
  Create directories `distribution/photoshop/macos/`, `distribution/photoshop/windows/`, and `release/`.
  Relocate with history preserved:
  (1) `git mv scripts/sync-version.js release/version.js`. Update its file-header comment to `release/version.js`. Add a one-line disambiguation comment near the top: it is the release-automation-scripts directory; built binary artifacts live in `releases/` (plural). Its logic is unchanged (reads package.json version → writes manifest.json + src/version.ts).
  (2) `git mv scripts/gh-release.js release/github-release.js`. Update its file-header comment to `release/github-release.js`. Add the same `release/` vs `releases/` disambiguation comment. Logic stays unchanged in this plan (the `.dmg` artifact path wiring happens in Plan 04 when the DMG build exists) — leave the existing `files` array as-is here.
  Update package.json: point the `prebuild` script to `node release/version.js`; point `publish:patch`/`publish:minor`/`publish:major` (and any script that referenced `scripts/gh-release.js`) to `release/github-release.js`. Do not change any other package.json scripts in this task.
  </action>
  <verify>
    <automated>test -d distribution/photoshop/macos && test -d distribution/photoshop/windows && test -f release/version.js && test -f release/github-release.js && node release/version.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `distribution/photoshop/macos/`, `distribution/photoshop/windows/`, and `release/` all exist
    - `release/version.js` and `release/github-release.js` exist; `scripts/sync-version.js` and `scripts/gh-release.js` no longer exist
    - `release/version.js` header comment reads `release/version.js` and contains the `release/` vs `releases/` disambiguation line
    - `git grep -n 'scripts/sync-version.js\|scripts/gh-release.js' package.json` returns nothing (all references updated)
    - `node release/version.js` runs and leaves src/version.ts VERSION equal to package.json version
    - `release/` contains NO checksums.js or gumroad-sync.js (not stubbed)
  </acceptance_criteria>
  <done>Directories exist; both host-agnostic scripts are relocated with updated headers and disambiguation comments; package.json references the new paths; build still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the existing Windows installer scripts unmodified</name>
  <files>distribution/photoshop/windows/install.bat, distribution/photoshop/windows/install.ps1, distribution/photoshop/windows/install.sh, distribution/photoshop/windows/uninstall.bat, distribution/photoshop/windows/uninstall.ps1</files>
  <read_first>
    - .planning/phases/01-foundation-macos-installer-rework/01-RESEARCH.md (Structure Rationale — Windows placeholder relocation rule)
    - .planning/phases/01-foundation-macos-installer-rework/01-PATTERNS.md (the `distribution/photoshop/windows/` relocation row)
  </read_first>
  <action>
  Relocate the existing Windows installer/uninstaller scripts merged in from origin/main into `distribution/photoshop/windows/` with their logic UNCHANGED, using `git mv`: `scripts/install.bat`, `scripts/install.ps1`, `scripts/install.sh`, `scripts/uninstall.bat`, `scripts/uninstall.ps1`. Do not edit their contents — Phase 2 (WIN-01..05) owns their rework. If any of these filenames is absent from the merged tree, skip it and note the absence in the SUMMARY rather than creating a new file. Do NOT relocate the legacy macOS `.pkg` scripts (scripts/build-mac-pkg.js, scripts/pkg-resources/*, scripts/build-mac-uninstaller.js) — they stay in scripts/ until Plans 03–04 supersede the installer side and Phase 3 rebuilds the uninstaller.
  </action>
  <verify>
    <automated>ls distribution/photoshop/windows/ | grep -E 'install|uninstall' && test ! -f scripts/install.bat</automated>
  </verify>
  <acceptance_criteria>
    - The Windows scripts that existed in the merged tree now live under `distribution/photoshop/windows/`
    - Their contents are byte-identical to the pre-move versions (verify with `git log --follow` / no content diff in the move commit)
    - Legacy macOS scripts (scripts/build-mac-pkg.js, scripts/pkg-resources/, scripts/build-mac-uninstaller.js) remain in scripts/ untouched
  </acceptance_criteria>
  <done>Windows scripts are relocated verbatim; macOS legacy scripts are left in place.</done>
</task>

<task type="auto">
  <name>Task 3: Add the three READMEs encoding the split's intent</name>
  <files>distribution/README.md, distribution/photoshop/macos/README.md, distribution/photoshop/windows/README.md</files>
  <read_first>
    - .planning/phases/01-foundation-macos-installer-rework/01-RESEARCH.md (Recommended Project Structure — the README purpose strings)
  </read_first>
  <action>
  Create three plain-language READMEs:
  (1) `distribution/README.md` — explains: one subfolder per host app (photoshop/ today; illustrator/ or figma/ would be siblings later); host-specific packaging/install lives here; the host-agnostic `release/` directory (checksum/GitHub-publish/Gumroad-sync automation) stays generic and is deliberately separate.
  (2) `distribution/photoshop/macos/README.md` — explains: macOS-specific installer; targets `~/Library/Application Support/Adobe/UXP/PluginsStorage/PHSP/...`; packaged as an unprivileged `.app` wrapped in a `.dmg` (built by Plan 04's build-installer.js); no root/admin required.
  (3) `distribution/photoshop/windows/README.md` — explains: the Windows installer rework lands in Phase 2 (WIN-01..05); the existing scripts were relocated here as-is; do not modify their logic in Phase 1.
  Each README must warn that `release/` (singular, automation scripts) is a different directory from `releases/` (plural, built binary output) — do not confuse the two.
  </action>
  <verify>
    <automated>test -f distribution/README.md && test -f distribution/photoshop/macos/README.md && test -f distribution/photoshop/windows/README.md && grep -l "Phase 2" distribution/photoshop/windows/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All three README files exist and are non-empty
    - distribution/photoshop/windows/README.md states the rework is Phase 2 and that logic must not be modified in Phase 1
    - distribution/photoshop/macos/README.md names the PluginsStorage/PHSP target path and the unprivileged .app/.dmg approach
    - Each README distinguishes `release/` (scripts) from `releases/` (binaries)
  </acceptance_criteria>
  <done>The three READMEs exist and encode the FOUND-02 split intent, including the release/releases disambiguation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → filesystem layout | Relocating scripts that build/package the plugin; a mislinked path would break the build or point release automation at the wrong directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `release/` (scripts) vs `releases/` (binaries) name collision misdirecting a build/publish glob | medium | mitigate | Mandatory disambiguation comment at the top of every `release/*.js` file and in all three READMEs; no glob may match `release*` |
| T-01-05 | Denial of Service (build breakage) | package.json still referencing the old `scripts/*.js` paths after the move | medium | mitigate | Update all package.json references in Task 1; verify with `npm run build` and a grep for stale paths |
</threat_model>

<verification>
- All three directories and both relocated scripts exist; old paths are gone.
- `node release/version.js` and `npm run build` still work after relocation.
- No stale `scripts/sync-version.js` / `scripts/gh-release.js` references remain in package.json.
- Windows scripts relocated with zero logic change; macOS legacy scripts untouched.
- No Phase 3/4 stub scripts created in `release/`.
</verification>

<success_criteria>
Completes the second half of the phase's Success Criterion 1: the repo has a `distribution/photoshop/{macos,windows}` + `release/` directory split ready for host-specific and release-automation work.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- New directories: `distribution/`, `distribution/photoshop/macos/`, `distribution/photoshop/windows/`, `release/`
- `release/version.js` (renamed from scripts/sync-version.js; same behavior, updated header + disambiguation comment)
- `release/github-release.js` (renamed from scripts/gh-release.js; unchanged logic this plan)
- Relocated Windows scripts under `distribution/photoshop/windows/` (install.bat, install.ps1, install.sh, uninstall.bat, uninstall.ps1 — as present in the merged tree)
- `distribution/README.md`, `distribution/photoshop/macos/README.md`, `distribution/photoshop/windows/README.md`
- Updated package.json script paths: `prebuild` → `node release/version.js`; `publish:*` → `release/github-release.js`
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation-macos-installer-rework/01-02-SUMMARY.md` when done. Note which Windows script filenames were actually present to relocate, and confirm the legacy macOS scripts were left in scripts/ intentionally.
</output>
