---
phase: 01-foundation-macos-installer-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - package.json
  - src/version.ts
  - webpack.config.js
  - .gitignore
autonomous: false
requirements: [FOUND-01]
must_haves:
  truths:
    - "`git status` after the merge shows a clean tree with zero unresolved conflict markers"
    - "`npm run build` succeeds on the merged branch"
    - "`npm test` passes on the merged branch"
    - "The working branch (epic/ui-icons) contains origin/main's installer commits through v1.6.2"
  artifacts:
    - "manifest.json resolved per the D-01a decision (schema version + host minVersion)"
    - "package.json carrying origin/main's publish:patch/minor/major scripts"
    - "src/version.ts regenerated from package.json version by the prebuild hook"
  key_links:
    - "package.json version stays in sync with src/version.ts and manifest.json via scripts/sync-version.js prebuild hook"
    - "epic/ui-icons .gitignore convention (ignore built release artifacts) is preserved, not regressed to origin/main's tracked-binaries convention"
---

<objective>
Merge `origin/main` (the shipped-but-flawed installer work, v1.6.1–1.6.2) into the current `epic/ui-icons` branch, resolving the five known file conflicts per the RESEARCH.md Merge Conflict Map, so all later Phase 1 work builds on a single consolidated tree (FOUND-01, D-01).

This plan does NOT restructure directories (that is Plan 02) and does NOT rebuild the installer (Plans 03–04). It brings origin/main's code onto the branch and leaves it buildable. Note explicitly: this merge briefly reintroduces the root-elevating `pkgbuild` `.pkg` installer flow and the PATH-trusting `postinstall` script into the working tree — this is expected and temporary; those files are superseded by the macOS installer rework later in this same phase (Plans 03–04). The branch must NOT be released in this intermediate state.

Purpose: Consolidate the diverged branches so the installer rework has one authoritative base.
Output: A merged, buildable, test-passing `epic/ui-icons` branch.
</objective>

<execution_context>
@/Users/hurisb/.claude/gsd-core/workflows/execute-plan.md
@/Users/hurisb/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-macos-installer-rework/01-CONTEXT.md
@.planning/phases/01-foundation-macos-installer-rework/01-RESEARCH.md
@.planning/phases/01-foundation-macos-installer-rework/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide manifest.json schema/minVersion resolution (D-01a)</name>
  <files>manifest.json</files>
  <action>Pause and present the decision below to the user before any merge conflict resolution. Do not resolve manifest.json in Task 2 until the user selects keep-v4 or take-v5. Record the selected option as decision D-01a in the SUMMARY.</action>
  <decision>How to resolve the `manifest.json` merge conflict: adopt origin/main's newer schema (which also raises the minimum Photoshop version) or keep the current branch's schema (which preserves older-Photoshop support).</decision>
  <context>
  origin/main's `manifest.json` sets `manifestVersion: 5`, drops the host `apiVersion` field, and raises host `minVersion` from `22.0.0` to `23.0.0`. The current epic/ui-icons branch has `manifestVersion: 4`, `apiVersion: 2`, `minVersion: 22.0.0`. The origin/main commit that made this change is titled "upgrade manifest to v5 for Adobe Developer Distribution" — but this milestone explicitly rejects Adobe Marketplace/Exchange distribution (see PROJECT.md constraints), so the reason that bump was made may not apply here. In plain terms: raising minVersion to 23.0.0 means anyone still on Photoshop 2022 could not run new versions.
  </context>
  <options>
    <option id="keep-v4">
      <name>Keep manifestVersion 4 / minVersion 22.0.0 (current branch)</name>
      <pros>Keeps Photoshop 2022 users supported; the reason v5 was added (Adobe's own distribution program) is out of scope for this milestone.</pros>
      <cons>If the local UXP packaging/build tooling later refuses v4, a follow-up bump may be needed. Verify by confirming `npm run build` and packaging succeed after choosing this.</cons>
    </option>
    <option id="take-v5">
      <name>Take manifestVersion 5 / minVersion 23.0.0 (origin/main)</name>
      <pros>Matches what already shipped in v1.6.2; forward-compatible with newer UXP tooling.</pros>
      <cons>Drops Photoshop 2022 support even though this milestone does not use Adobe's distribution channel that required the bump.</cons>
    </option>
  </options>
  <resume-signal>Select: keep-v4 or take-v5 (record the choice as decision D-01a in the SUMMARY)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Merge origin/main into epic/ui-icons and resolve the five conflicts</name>
  <files>manifest.json, package.json, src/version.ts, webpack.config.js, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-macos-installer-rework/01-RESEARCH.md (the `## FOUND-01: Merge Conflict Map` table — per-file resolution)
    - manifest.json (current branch state, to apply the D-01a decision)
    - package.json (current branch state, missing publish:* scripts)
    - src/version.ts (current branch state)
    - webpack.config.js (current branch state)
    - .gitignore (current branch state — the newer artifact-ignoring convention to preserve)
  </read_first>
  <action>
  On branch `epic/ui-icons`, run `git merge origin/main`. Resolve exactly the five conflicting files per the RESEARCH.md Merge Conflict Map:
  (1) manifest.json — apply the D-01a decision from Task 1 (keep-v4 or take-v5); resolve manifestVersion, host apiVersion presence, and minVersion together consistently with that choice.
  (2) package.json — take origin/main's `publish:patch`, `publish:minor`, `publish:major` scripts (they are needed for later release automation and cost nothing now); keep the current branch's existing scripts otherwise; take origin/main's `version` value (1.6.2).
  (3) src/version.ts — take origin/main's value; it is regenerated from package.json by the `prebuild` hook (scripts/sync-version.js) on every build, so only a valid starting value matters.
  (4) webpack.config.js — take origin/main's version (adds `globOptions: { ignore: ['**/.DS_Store'] }` to the icons CopyWebpackPlugin pattern — a hygiene improvement, no conflict with epic/ui-icons work).
  (5) .gitignore — KEEP the epic/ui-icons convention that ignores built release artifacts (`releases/`, `*.ccx`, `!.planning/**/*.md`); do NOT regress to origin/main tracking `releases/*` binaries in git. This matches the milestone principle that GitHub Releases is the canonical file host.
  After committing the merge, untrack the binary release artifacts that origin/main committed but the kept .gitignore now excludes: run `git rm --cached` on the tracked files under `releases/` (this untracks them going forward without rewriting shared history), then commit that cleanup. Do NOT `git rm` (working-tree deletion) and do NOT rewrite history.
  Do NOT modify, relocate, or delete any of the merged-in installer scripts in this plan (scripts/build-mac-pkg.js, scripts/pkg-resources/*, scripts/build-mac-uninstaller.js, scripts/install.*, scripts/uninstall.*, scripts/gh-release.js) — relocation is Plan 02 and the macOS rework is Plans 03–04.
  </action>
  <verify>
    <automated>git diff --check && test -z "$(git ls-files -u)" && echo "no conflict markers, no unmerged paths"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -u` returns empty (no unmerged paths remain)
    - `git diff --check` reports no conflict markers
    - `git log --oneline` on epic/ui-icons includes origin/main's release commits through v1.6.2 (e.g. `chore: release v1.6.2`)
    - manifest.json matches the D-01a decision exactly (schema version, apiVersion presence, minVersion all consistent)
    - package.json contains `publish:patch`, `publish:minor`, `publish:major` scripts
    - .gitignore still ignores `releases/` and `*.ccx`; `git ls-files releases/` returns empty after the `git rm --cached` cleanup
  </acceptance_criteria>
  <done>Merge is committed with no unresolved conflicts; manifest.json reflects the Task 1 decision; release artifacts are untracked; the branch history contains origin/main's installer work.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the merged branch builds and tests pass</name>
  <files>src/version.ts (regenerated by the prebuild hook; no other writes — runs npm run build && npm test)</files>
  <read_first>
    - package.json (scripts section — build, prebuild, test)
  </read_first>
  <action>
  Run `npm install` (to pick up any dependency changes merged from origin/main), then `npm run build` and `npm test`. Both must succeed. If `npm run build` fails specifically because of the manifest schema (and Task 1 chose keep-v4), record that as evidence favoring take-v5 and re-open the D-01a decision with the finding rather than silently changing it. Do not add features or fix unrelated failures here — the only goal is a green build+test on the consolidated tree.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces dist/ output
    - `npm test` exits 0 (existing gridGenerator suite passes)
    - src/version.ts VERSION matches package.json version after the prebuild hook runs
  </acceptance_criteria>
  <done>The merged branch builds and passes the existing test suite with the chosen manifest resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| origin/main (remote branch) → local working tree | Merging brings previously-shipped installer code, including the root-elevating `.pkg` flow, onto the active branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Merged-in `scripts/build-mac-pkg.js` + `scripts/pkg-resources/postinstall` (root `.pkg` flow) reintroduced into the tree | high | mitigate | Superseded within the same phase by the unprivileged `.app` installer (Plans 03–04); branch is NOT released in this intermediate state; SUMMARY flags the temporary reintroduction |
| T-01-02 | Tampering | Merged-in `postinstall` uses bare `$PATH`-trusting command names (PATH hijacking surface) | high | mitigate | Not consumed by any new build path this plan; replaced by absolute-path installer in Plans 03–04; left untouched here only as transient state |
| T-01-03 | Information Disclosure | origin/main's tracked `releases/*` binary artifacts entering git history/bloat | low | mitigate | `git rm --cached` untracks them going forward; kept `.gitignore` prevents re-tracking; no history rewrite (avoids shared-history risk) |
</threat_model>

<verification>
- Merge produces a clean tree: `git ls-files -u` empty, `git diff --check` clean.
- Consolidated branch is buildable and green: `npm run build && npm test`.
- manifest.json resolution is consistent with the recorded D-01a decision.
- Release binaries are untracked; `.gitignore` convention preserved.
</verification>

<success_criteria>
Success Criterion 1 of the phase ("the working branch contains all of origin/main's installer work merged in with no unresolved conflicts") is satisfied for the merge half. The directory split (the second half of Criterion 1) is delivered by Plan 02.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
- Merged `epic/ui-icons` branch containing origin/main commits through v1.6.2 (no new source symbols)
- Resolved `manifest.json` (schema/minVersion per D-01a)
- `package.json` with `publish:patch` / `publish:minor` / `publish:major` scripts
- Regenerated `src/version.ts` (`export const VERSION`)
- Untracked (via `git rm --cached`) `releases/*` binary artifacts
- Decision record: **D-01a** (manifest schema/minVersion resolution)
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation-macos-installer-rework/01-01-SUMMARY.md` when done. Record the D-01a decision and note the temporary reintroduction of the root `.pkg` flow (to be superseded by Plans 03–04).
</output>
