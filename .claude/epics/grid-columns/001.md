---
name: Project setup — estructura base del plugin
status: open
size: S
stream: A
epic: grid-columns
created: 2026-03-06T18:35:11Z
updated: 2026-03-06T18:35:11Z
depends_on: []
github:
---

# Task 001: Project setup — estructura base del plugin

## Descripción

Inicializar la estructura de carpetas, archivos de configuración y dependencias del plugin UXP. Al terminar esta task, el proyecto debe compilar con `npm run build` y el plugin debe cargarse en Adobe UXP Developer Tool (aunque no haga nada todavía).

## Archivos a crear

```
guidemygrid/
├── manifest.json
├── index.html
├── package.json
├── webpack.config.js
├── tsconfig.json
├── src/
│   └── index.tsx          ← React entry point mínimo
└── icons/
    ├── icon-23.png        ← 23×23 px (placeholder OK para dev)
    └── icon-96.png        ← 96×96 px (placeholder OK para dev)
```

## Implementación

### manifest.json
Copiar exactamente de `docs/reference/technical-decisions.md` → Sección 14.

**Crítico:** verificar que `"network": { "domains": ["https://api.github.com"] }` esté incluido.

### webpack.config.js
Copiar de `docs/reference/technical-decisions.md` → Sección 13.2.

**Crítico:** los `externals` deben incluir `photoshop`, `uxp`, `os`, `fs`, `path`. Sin esto el plugin no funciona.

### tsconfig.json
Copiar de `docs/reference/technical-decisions.md` → Sección 13.1.

### package.json
Copiar dependencias de `docs/reference/technical-decisions.md` → Sección 2.6.

Agregar scripts:
```json
{
  "name": "guidemygrid",
  "version": "1.0.0",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit",
    "test": "jest"
  }
}
```

### index.html
```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>GuideMyGrid</title>
  </head>
  <body>
    <div id="root"></div>
    <script src="index.js"></script>
  </body>
</html>
```

### src/index.tsx (mínimo para compilar)
```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <div>GuideMyGrid</div>;
}

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);
```

## Acceptance Criteria

- [ ] `npm install` sin errores
- [ ] `npm run build` genera `dist/index.js` + `dist/index.html` + `dist/manifest.json`
- [ ] Adobe UXP Developer Tool puede cargar el plugin desde `dist/`
- [ ] El panel aparece en Photoshop → Plugins → GuideMyGrid (aunque muestre solo "GuideMyGrid")
- [ ] `npm run type-check` sin errores TypeScript
