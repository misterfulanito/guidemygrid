---
name: SideGuidesBar — componente UI
status: open
size: M
stream: B
created: 2026-03-10T22:57:08Z
updated: 2026-03-10T22:57:08Z
depends_on: [002]
---

# SideGuidesBar — Componente UI

## Objetivo

Crear el componente `SideGuidesBar` con 6 botones de acción inmediata usando iconos de `lucide-react`.

## Archivos a crear

- `src/components/SideGuidesBar/SideGuidesBar.tsx`
- `src/components/SideGuidesBar/SideGuidesBar.module.css`

## Props

```typescript
interface SideGuidesBarProps {
  containerWidth: number;
  containerHeight: number;
  offsetX: number;
  offsetY: number;
  disabled?: boolean; // true cuando no hay documento activo
}
```

## Estructura del componente

```tsx
import {
  AlignStartVertical, AlignCenterVertical, AlignEndVertical,
  AlignStartHorizontal, AlignCenterHorizontal, AlignEndHorizontal
} from 'lucide-react';
import { generateSideGuide, SideGuideType } from '../../services/gridGenerator';
import { photoshopBridge } from '../../services/photoshopBridge';

// 6 botones definidos como config array
const BUTTONS = [
  { type: 'left',     Icon: AlignStartVertical },
  { type: 'center-v', Icon: AlignCenterVertical },
  { type: 'right',    Icon: AlignEndVertical },
  { type: 'top',      Icon: AlignStartHorizontal },
  { type: 'center-h', Icon: AlignCenterHorizontal },
  { type: 'bottom',   Icon: AlignEndHorizontal },
] as const;
```

### Estado por botón

```typescript
const [loading, setLoading] = useState<SideGuideType | null>(null);
```

### Handler

```typescript
const handleClick = async (type: SideGuideType) => {
  if (disabled || loading) return;
  setLoading(type);
  try {
    const { position, orientation } = generateSideGuide(type, {
      containerWidth, containerHeight, offsetX, offsetY
    });
    await photoshopBridge.addGuide(position, orientation);
  } catch {
    // silencioso — no bloquea UI
  } finally {
    setLoading(null);
  }
};
```

### CRÍTICO — UXP: Icono en span dentro del button

```tsx
<button
  key={type}
  className={`${styles.btn} ${loading === type ? styles.btnLoading : ''}`}
  disabled={disabled || loading !== null}
  onClick={() => handleClick(type)}
>
  <span className={styles.iconWrap}>
    <Icon size={16} strokeWidth={1.5} />
  </span>
</button>
```

## CSS (tokens del proyecto)

```css
.bar {
  display: flex;
  flex-direction: row;
  gap: 4px;
  padding: 8px 12px;
  background: #1A1A1A;
  border-bottom: 1px solid #2A2A2A;
}

.btn {
  flex: 1;
  height: 28px;
  background: #2D2D2D;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #FFFFFF;
}

.btn:hover:not(:disabled) { background: #3D3D3D; }
.btn:active:not(:disabled) { background: #FF6B35; }
.btn:disabled { opacity: 0.4; cursor: default; }
.btnLoading { background: #3D3D3D; }

.iconWrap {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
}
```

## Acceptance Criteria

- [ ] 6 botones renderizados en una fila
- [ ] Iconos visibles (span wrapper, no directo en button)
- [ ] Clic crea guía inmediatamente (additive)
- [ ] Estado loading por botón durante operación
- [ ] Todos los botones disabled cuando `disabled=true`
- [ ] Estilos siguen design tokens del proyecto
