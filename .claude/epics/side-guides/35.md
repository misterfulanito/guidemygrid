---
name: generateSideGuide — servicio y tests
status: open
size: S
stream: A
created: 2026-03-10T22:57:08Z
updated: 2026-03-10T23:18:05Z
github: https://github.com/misterfulanito/guidemygrid/issues/35
depends_on: [34]
---

# generateSideGuide — Servicio y Tests

## Objetivo

Añadir `generateSideGuide()` a `src/services/gridGenerator.ts` y sus tests unitarios.

## Implementación

### Tipo

```typescript
export type SideGuideType =
  | 'left' | 'center-v' | 'right'
  | 'top'  | 'center-h' | 'bottom';
```

### Función

```typescript
export function generateSideGuide(
  type: SideGuideType,
  params: {
    containerWidth: number;
    containerHeight: number;
    offsetX: number;
    offsetY: number;
  }
): { position: number; orientation: 'vertical' | 'horizontal' } {
  const { containerWidth, containerHeight, offsetX, offsetY } = params;

  switch (type) {
    case 'left':     return { position: Math.round(offsetX * 100) / 100,                                   orientation: 'vertical' };
    case 'center-v': return { position: Math.round((offsetX + containerWidth / 2) * 100) / 100,            orientation: 'vertical' };
    case 'right':    return { position: Math.round((offsetX + containerWidth) * 100) / 100,                orientation: 'vertical' };
    case 'top':      return { position: Math.round(offsetY * 100) / 100,                                   orientation: 'horizontal' };
    case 'center-h': return { position: Math.round((offsetY + containerHeight / 2) * 100) / 100,          orientation: 'horizontal' };
    case 'bottom':   return { position: Math.round((offsetY + containerHeight) * 100) / 100,              orientation: 'horizontal' };
  }
}
```

### Tests

Crear `src/__tests__/gridGenerator.sideGuide.test.ts`:
- Test sin selección: offsetX=0, offsetY=0, canvas 1920×1080
  - left → { position: 0, orientation: 'vertical' }
  - center-v → { position: 960, orientation: 'vertical' }
  - right → { position: 1920, orientation: 'vertical' }
  - top → { position: 0, orientation: 'horizontal' }
  - center-h → { position: 540, orientation: 'horizontal' }
  - bottom → { position: 1080, orientation: 'horizontal' }
- Test con selección: offsetX=100, offsetY=50, width=800, height=600
  - left → 100, center-v → 500, right → 900
  - top → 50, center-h → 350, bottom → 650

## Acceptance Criteria

- [ ] `SideGuideType` exportado desde gridGenerator.ts
- [ ] `generateSideGuide()` exportado desde gridGenerator.ts
- [ ] Todos los tests pasan con `npm test`
